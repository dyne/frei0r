Shadert0y

A filter plugin that runs Shadertoy GLSL shaders inside Kdenlive, or other MLT-based hosts.
The shader is rendered offscreen using EGL + OpenGL 3.3 core, then written back to the host video frame.

Created by YousufPro67 (HyperDev)
